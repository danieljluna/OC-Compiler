(1 + 2);
(2 - 'a');
(4 * -2);
(5 / 2 % 3);

(true == (5 < 6));

(true == ("12" != "ab"));

char a = chr ord chr 5;


struct a {
   int one;
   int two;
   char three;
}

/*
string[] argv = getargv ();
int argi = 1;
while (argv[argi] != null) {
   if (argi > 1) putc (' ');
   puts (argv[argi]);
   argi = argi + 1;
}
endl ();

*/