struct foo {
   bool [] a;
   void n;
}

bool bob(int a, bool b);

{
   bool b = bob(10, true);
   int c = new int [-5];
   blah xkcd = new blah();
}


bool bob(int a, bool b) {
   char c = 'c';
   a = 5;
}
