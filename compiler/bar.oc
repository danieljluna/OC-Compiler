int a = 1;
bool b = 0;


struct blah {
   bool foo;
   blah bar;
   bool [] boolArray;
}

/*
if (true) (new string(5))[1] = 'a';


b = new bool[10];
c = new string(56);
a = new blah();
a.foo;
a[b];
a.so;

char a = chr ord chr 5;

/*
struct a {
   int one;
   int two;
   char three;
}

/*
string[] argv = getargv ();
int argi = 1;
while (argv[argi] != null) {
   if (argi > 1) putc (' ');
   puts (argv[argi]);
   argi = argi + 1;
}
endl ();

*/